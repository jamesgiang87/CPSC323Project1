! This function sums two numbers !
function sumFunc[num1$:int, num2$:int]
{
	return num1$ + num2$;
}

%%

! numbers that we want to sum !
int number1$, number2$, sum;	

get(number1$, number2$);
sum = sumFunc(number1$, number2$);

put(sum);

