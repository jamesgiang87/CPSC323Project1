! This function sums two numbers !
function compare[num1$:int, num2$:int]
{
	if (num1$ > num2$)
	{
		return num1$;
	}

	return num2$;
}

%%

! numbers that we want to sum !
int number1$, number2$, largestNum;	

get(number1$, number2$);
largestNum = compare(number1$, number2$);

put(largestNum);

