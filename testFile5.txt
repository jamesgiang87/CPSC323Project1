! This program will compute the factorial of a desired number !

function factorial [number:int]
{
    if (number == 1)
    {
        return 1;
    }
    else
    {
        number = number - 1;
        return (number + 1) * factorial(number);
    }
    endif
}

function otherFunc []
{
    return 777;
}

function otherotherFunc[num:int, nums:real, bool:boolean]
{
    return;
}

%%

! Get the factorial that the user wants to compute !
int factorialNum;
boolean continueLoop;

{
    factorialNum = 0;
    continueLoop = true;

    while (continueLoop == true)
    {
        get(factorialNum);

        if (factorialNum => 1)
        {
            continueLoop = false;
        }
        endif

        ! compute the factorial !
        put(factorial(factorialNum));
    }
}




